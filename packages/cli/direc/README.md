# direc

This is the CLI entry point for `direc` — the publishable package that users invoke with `npx direc` or install globally via `npm install -g direc`.

See the [monorepo README](../../../README.md) for full documentation on getting started, analysis scenarios, and automation.
